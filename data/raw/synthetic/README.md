# Raw Synthetic Data

This directory is reserved for source synthetic records before normalization. The current small UI/demo generator remains at `data/synthetic/index.js`; the larger ML generator is at `data/pipeline/generateSyntheticML.mjs`.

Generated ML outputs are written to `data/processed/` and remain explicitly labelled `SYNTHETIC`.
